extends ../layouts/base


block head
	- var CURRENT = "404";
	- var WRAPPER_CLASS = "";


block modal
	include ../sections/rule-modal

block content
	section#missing
		.container
			.wrap
				.title-group
					h1 404
					.tagline Page not found.
				
				a(href="/").button Back to shop

	include ../sections/footer